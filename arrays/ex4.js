/*Declare an array named `citiesVisited` containing
`"Mumbai"` and Sydney. Add "Berlin" to the array using
the `push` method. */

var citiesVisited = ["mumbai","sydney"]
citiesVisited.splice(1,0,"berlin")
console.log(citiesVisited)